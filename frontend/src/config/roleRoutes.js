// Central mapping from role names to their default landing routes
// Update this single source when introducing new roles or routes
const ROLE_ROUTES = {
  student: "/student",
  teacher: "/teacher",
};

export default ROLE_ROUTES;
